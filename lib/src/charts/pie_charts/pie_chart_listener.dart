import '../common/radial_observer.dart';

class PieChartListener extends RadialObserver {
  PieChartListener(double minValue, double maxValue)
      : super(minValue: minValue, maxValue: maxValue);
}
